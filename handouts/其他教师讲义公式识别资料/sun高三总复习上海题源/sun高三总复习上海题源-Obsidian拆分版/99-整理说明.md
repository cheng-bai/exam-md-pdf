# 整理说明

## 已完成

- 从 `sun高三总复习上海题源-dollar.md` 拆分出 66 个 Markdown 文件。
- 已复制被正文引用的图片 114 张到 `assets/images/`。
- 所有拆分文件均使用相对图片路径，便于在 Obsidian 中移动整个目录。
- 每个拆分文件增加了 frontmatter，标记来源、模块与 `OCR待校对` 状态。

## 后续建议

- 优先校对 `\parallel` 被误识别为集合竖线、`\Phi` 与 `\phi` 混用、中文断裂等 OCR 问题。
- 若后续要精修，可按模块逐讲把“知识点梳理 / 例题精讲 / 巩固练习”进一步统一为固定标题。
- `assets/未引用图片清单.txt` 中的图片未被当前 Markdown 正文引用，暂未删除。
